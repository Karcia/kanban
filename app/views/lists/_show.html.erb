<div class="list-container col-lg-4">
  <div class="panel panel-default">
    <div class="panel-heading list">
      <h4>
        <%= link_to list.title, edit_project_list_path(project_id: list.project.id, id: list.id) %>
      </h4>
    </div>
    <div class="descr">
      <%= list.description.truncate_words(10) if list.description.present? %>
    </div>

    <%= render 'cards/index', cards: list.cards  %>

    <div style= "text-align: right">
      <%= link_to 'x', project_list_path(project_id: list.project.id, id: list.id), method: :delete, data: { confirm: 'Are you sure?' } %>
    </div>
    <div class="panel-footer">
      <%= link_to 'Add card', new_project_list_card_path(project_id: list.project.id, list_id: list.id) %>
    </div>
  </div>
</div>